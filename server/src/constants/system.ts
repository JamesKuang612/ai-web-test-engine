/** 日志文件默认保留天数。 */
export const DEFAULT_LOG_ROTATE_DAYS = 14;

/** 收到关闭信号后，为探针状态传播预留的等待时间。 */
export const SHUTDOWN_WAIT_MS = 10000;

/** HTTP Server 优雅关闭失败后的最大等待时间。 */
export const HTTP_SHUTDOWN_MS = 20000;
